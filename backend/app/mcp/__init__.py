"""MCP adapter (#8): a dependency-free Model Context Protocol server (JSON-RPC 2.0 over
stdio) whose tools/resources call the ResearchMind capability layer — never the database or
duplicated logic (spec §25, §55). Run with ``python -m app.mcp``.
"""
