"""Personal knowledge base: semantic research memory, reuse, and knowledge graph
(spec §14, §15). Qdrant runs in embedded local mode (on-disk, no server) so it fits
the lean-local setup; embeddings come from the configured Ollama model.
"""
